# OpenBurn Terminology

- OpenUsage uses the term `plugin`.
- OpenBurn uses the term `provider` for the same concept.
- When porting code from OpenUsage to OpenBurn, rename domain terms accordingly (`Plugin*` -> `Provider*`, `pluginIds` -> `providerIds`, etc.).
